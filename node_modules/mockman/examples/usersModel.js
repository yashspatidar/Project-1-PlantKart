/**
 * Example for MockMan
 */

var persistence_layer = require('./usersPersistenceLayer');

module.exports = {
	name = '',
	user_name = '',

	getName: function() {
		return name;
	},

	getUserName: function() {
		return user_name;
	},

	setName: function(new_name) {
		this.name = new_name;
	},

	setUserName: function(new_name) {
		this.user_name = new_name;
	}

	save: function(callback) {
		var obj = { userName: user_name, realname: name };
		persistence_layer.save(obj, function(err, result) {
			if(err) {
				return callback(err, null);
			}
			return callback(null, { userName: obj.userName });
		});
	}

};